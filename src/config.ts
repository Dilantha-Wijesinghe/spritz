/* Calibration constants — values from the design's props panel. */
export const SPRAYS_PER_ML = 10;
/** Bottles at or below this remaining % count as "running low". */
export const LOW_PCT = 15;
export const DEFAULT_SPRAYS = 3;
export const STORAGE_KEY = 'spritz.tracker.v1';
